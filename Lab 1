# Завдання 1
# Створюємо 4 змінні та вводимо їх значення з клавіатури

a = float(input("Введіть перше число (ціле або дробове): "))
b = float(input("Введіть друге число (ціле або дробове): "))
c = float(input("Введіть третє число (ціле або дробове): "))
d = float(input("Введіть четверте число (ціле або дробове): "))

# Завдання 2
# Виконуємо математичні операції

addition = a + b
subtraction = c - d
multiplication = a * c
division = b / d if d != 0 else 'Ділення на нуль'
power = a ** b
integer_division = c // a if a != 0 else 'Цілочисленне ділення на нуль'
modulo = d % b if b != 0 else 'Остача від ділення на нуль'

# Записуємо результати у список

results = [addition, subtraction, multiplication, division, power, integer_division, modulo]

# Завдання 3
# Визначаємо кількість елементів у списку

length_of_list = len(results)
print(f"\nКількість елементів у списку: {length_of_list}")

# Виводимо парні елементи (ті, які є числами і діляться на 2 без остачі)

print("\nПарні елементи списку:")
for element in results:
    if isinstance(element, (int, float)):  # Перевіряємо, що це число
        if element % 2 == 0:
            print(element)

# Завдання 4
# Міняємо місцями другий і п’ятий елементи списку (індекси 1 і 4)

results[1], results[4] = results[4], results[1]

# Виводимо оновлений список

print("\nОновлений список після обміну 2-го і 5-го елементів:")
print(results)

# Завдання 5
# Вводимо прізвище та ім’я, виводимо повідомлення та висновки

name = input("\nВведіть ваше прізвище та ім'я: ")

print("\nВиконавець лабораторної роботи:")
print(name)

print("\nВисновки по лабораторній роботі:")
print("1. Навчився(-лася) використовувати функцію input для введення даних.")
print("2. Закріпив(-ла) навички роботи з основними математичними операціями.")
print("3. Практикував(-ла) роботу зі списками та маніпуляціями їх елементів.")
